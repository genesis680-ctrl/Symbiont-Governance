name: 🏛️ Symbiont Governance Check

on: [push, pull_request]

jobs:
  governance-audit:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v3
      
    - name: 🐍 Setup Python Engine
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 🛡️ Run Governance Validator
      run: |
        python engine/validator.py

